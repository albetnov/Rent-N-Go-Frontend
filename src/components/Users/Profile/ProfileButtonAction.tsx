import { Button, type ButtonProps } from '@chakra-ui/react'

export default function ProfileButtonAction(props: ButtonProps) {
  return <Button {...props} variant="outline" w="fit" rounded="full" p="0" />
}
